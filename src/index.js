import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import AimTrainer from './App';

ReactDOM.render(
  <React.StrictMode>
    <AimTrainer />
  </React.StrictMode>,
  document.getElementById('root')
);
